<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Edit Post</title>
  </head>
  <body>
    <h1>Edit <%= @post.name %></h1>
    <form action="/posts/<%= @post.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <input type="text" name="post[name]" value="<%= @post.name %>" placeholder="name">
      <input type="text" name="post[content]" value="<%= @post.content %>" placeholder="content">
      <input type="submit" value="submit">
    </form>
  </body>
</html>
