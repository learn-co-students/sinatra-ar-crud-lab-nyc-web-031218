<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @posts.each{|post| %>
   <%= post.name %>
    <%= post.content %> 
  <%  } %>
  </body>
</html>
